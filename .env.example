# Database
DATABASE_URL="postgresql://sis:sis_dev_password@localhost:5432/sis"
POSTGRES_USER=sis
POSTGRES_PASSWORD=sis_dev_password
POSTGRES_DB=sis
POSTGRES_PORT=5432

# Redis
REDIS_URL="redis://localhost:6379"
REDIS_PORT=6379

# MinIO (S3-compatible storage)
MINIO_ROOT_USER=sis_minio
MINIO_ROOT_PASSWORD=sis_minio_password
MINIO_PORT=9000
MINIO_CONSOLE_PORT=9001
S3_ENDPOINT="http://localhost:9000"
S3_ACCESS_KEY=sis_minio
S3_SECRET_KEY=sis_minio_password
S3_BUCKET_DOCUMENTS=documents
S3_BUCKET_TRANSCRIPTS=transcripts
S3_BUCKET_FINANCIAL=financial-docs
S3_BUCKET_PHOTOS=photos

# Next.js
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# NextAuth.js v5
AUTH_URL="http://localhost:3000"
AUTH_SECRET="your-development-secret-change-in-production"
# Generate with: openssl rand -base64 32

# Email (Mailpit for development)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM="noreply@sis.local"

# Application
NODE_ENV=development
LOG_LEVEL=debug

# Institution (for single-tenant setup)
INSTITUTION_CODE=DEMO
INSTITUTION_NAME="Demo University"
